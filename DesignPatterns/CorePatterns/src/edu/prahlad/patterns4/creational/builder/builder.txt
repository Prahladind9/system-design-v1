Builder pattern encapsulates or hides the process of building a complex object and
separated the representation of the object and its construction.
The separation allows us to construct different representation using the same construction process.